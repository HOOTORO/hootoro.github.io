sitename     = "local.exp"
defaultColor = "dark"      # set color mode: dark, light, auto
# You can take a look at layouts/index.html for more information.
description = "light and dark support each other"
mainSections = [
    'post',
]
toc = true # set to false to disable table of contents 'globally'
tocOpen = false # set to true to open table of contents by default
goToTop = true # set to false to disable 'go to top' button
additionalScripts = [
    'js/custom.js',
]
# Will try to load 'assets/js/custom.js' and 'assets/js/custom-2.js'.
# Your custom scripts will be concatenated to one file `custom.js`.
# When building for production it will be minified.
# The file `custom.js` is loaded on each page (before body tag ends).
dateFormat = "" # date format used to show dates on various pages. If nothing is specified, then "2 Jan 2006" format is used.
# See https://gohugo.io/functions/format/#hugo-date-and-time-templating-reference for available date formats.
rssFeedDescription = "summary" # available options: 1) summary 2) full
# summary - includes a short summary of the blog post in the RSS feed. Generated using Hugo .Summary .
# full - includes full blog post in the RSS feed. Generated using Hugo .Content .
# By default (or if nothing is specified), summary is used.
subtitle = 'The Best Bad on Earth'
[author]
    avatar = "avatar.png" # put the file in assets folder; also ensure that image has same height and width
    # Note: image is not rendered if the resource(avatar image) is not found. No error is displayed.
    intro              = 'LOCAL.EXP'
    name               = 'HOOTORO'
    description        = 'A bad dev, ok friend, running fast, minimalistic, personal blog powered by Hugo Framework. For post boring stories and share ordinary thoughts based on personal experience. There is nothing interesting.'
    creation           = '18.07.2023'
    SidebarRecentLimit = 5
[contact]
    email = "marukubaka@gmail.com"
# Allow to override webmanifest options
[webmanifest]
    name             = "HOOT"   # will use "params.sitename" or "title" by default
    short_name       = "sitename"   # same as name
    start_url        = "/"          # will use homepage url by default
    theme_color      = "#434648"    # default is "#434648" (base color of text). Also will override html `<meta name="theme-color" />`
    background_color = "#fff"       # by default depend on "params.defaultColor" for "light" or "auto" will be set to "#fff" for dark will be "#131418" (color of dark mode background)
    display          = "standalone"
    [[webmanifest.icons]]
        src = '/icons/favicon.ico'
        sizes = '256x256'
        type = "image/png"

[browserconfig]
    TileColor = "#2d89ef" # default windows 10 blue tile color

[[socialIcons]]
    name = "github"
    url  = "https://github.com/HOOTORO/"

[[socialIcons]]
    name = "discord"
    url  = "https://discordid.netlify.app/?id=88370628763078656"

[[socialIcons]]
    name = "lastfm"
    url  = "https://www.last.fm/user/marukubaka"
[[socialIcons]]
    name = "Rss"
    url  = "index.xml"
